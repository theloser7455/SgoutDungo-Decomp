if (sprite_index == spr_beam2)
    image_angle += (2 * image_yscale);

if (floor(image_index) == (image_number - 1) && sprite_index == spr_beam3)
    sprite_index = spr_beam2;

x = obj_snickexe.x;
y = obj_snickexe.y;
